# Helm values for kube-prometheus-stack
# This configures Prometheus to discover ServiceMonitors across all namespaces

prometheus:
  prometheusSpec:
    # Allow Prometheus to discover ServiceMonitors in all namespaces
    serviceMonitorSelectorNilUsesHelmValues: false
    podMonitorSelectorNilUsesHelmValues: false
    
    # Retention policy (adjust based on storage capacity)
    retention: 15d
    
    # Resource limits
    resources:
      requests:
        memory: 2Gi
        cpu: 1000m
      limits:
        memory: 4Gi
        cpu: 2000m
    
    # Storage configuration (adjust based on your storage class)
    storageSpec:
      volumeClaimTemplate:
        spec:
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 50Gi

grafana:
  # Grafana configuration
  adminUser: admin
  adminPassword: prom-operator
  
  # Disable init container (fixes Windows/Docker Desktop chown issue)
  initChownData:
    enabled: false
  
  # Persistence
  persistence:
    enabled: true
    size: 10Gi
  
  # Resources
  resources:
    requests:
      memory: 256Mi
      cpu: 100m
    limits:
      memory: 512Mi
      cpu: 200m
  
  # Additional volumes for dashboard provisioning (applied via ConfigMaps)
  extraVolumes:
    - name: dashboards
      configMap:
        name: grafana-dashboards
    - name: dashboard-provider
      configMap:
        name: grafana-dashboard-provider
    - name: datasource
      configMap:
        name: grafana-datasource
  
  extraVolumeMounts:
    - name: dashboards
      mountPath: /var/lib/grafana/dashboards/fastapi-ml
    - name: dashboard-provider
      mountPath: /etc/grafana/provisioning/dashboards
    - name: datasource
      mountPath: /etc/grafana/provisioning/datasources

# AlertManager configuration
alertmanager:
  alertmanagerSpec:
    resources:
      requests:
        memory: 100Mi
        cpu: 100m
      limits:
        memory: 200Mi
        cpu: 200m



